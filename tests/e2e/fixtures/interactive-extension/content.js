// Content script: logs once per page load so network/console panels show real
// activity when the user navigates the interactive browser.
console.log("[interactive-fixture] content script injected");
